param(
    [int]$RebuildPid = 0
)

Write-Host "Deprecated: safe_rebuild_watcher is disabled for database-first runtime."
Write-Host "Cache rebuilds are optional acceleration and are not supervised here."
exit 0
