# Full-corpus training via pre-built feature cache.
# Build the cache first: python training/build_feature_cache.py
# Then run: titanium train --config training/configs/value_nnue_oracle_long.yaml
data: training/data/teacher_dataset
cache_dir: training/data/feature_cache
out_dir: training/runs/value_oracle
epochs: 50
patience: 15
batch: 512
lr: 0.001
checkpoint_steps: 1000
val_split: 0.05
min_val: 64
seed: 0

teacher_dataset: training/data/teacher_dataset
active_manifest_sha256: 31a422f25a8c701ebfa72410f59fab9dff52c2717e30985a3f8e6929be007d02

lmr_ready: false
