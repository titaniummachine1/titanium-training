[pytest]
pythonpath = .
markers =
    integration: requires production data on disk (teacher store + JSONL corpus)
    slow: expected runtime > 60 s; not included in the fast unit suite
addopts = --basetemp=.pytest-temp
testpaths = tests
